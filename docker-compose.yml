services:
  postgres:
    image: postgres:16-alpine
    container_name: paobom-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-paobom}
      POSTGRES_USER: ${POSTGRES_USER:-paobom}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-paobom}
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      - paobom_postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $${POSTGRES_USER} -d $${POSTGRES_DB}"]
      interval: 10s
      timeout: 5s
      retries: 5

  pgadmin:
    image: dpage/pgadmin4:8
    container_name: paobom-pgadmin
    restart: unless-stopped
    profiles:
      - tools
    environment:
      PGADMIN_DEFAULT_EMAIL: ${PGADMIN_DEFAULT_EMAIL:-admin@paobom.local}
      PGADMIN_DEFAULT_PASSWORD: ${PGADMIN_DEFAULT_PASSWORD:-paobom}
    ports:
      - "${PGADMIN_PORT:-5050}:80"
    depends_on:
      postgres:
        condition: service_healthy
    volumes:
      - paobom_pgadmin_data:/var/lib/pgadmin

volumes:
  paobom_postgres_data:
  paobom_pgadmin_data:
